apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: demo-app
  namespace: default
spec:
  values:
    clusterName: "amsterdam"
    image:
      tag: "2.0.0"
    gateway:
      enabled: true
      hostname: "amsterdam.demo.qws.xyz"
      mcsHostname: "amsterdam-mcs.demo.qws.xyz"
      headlessHostname: "amsterdam-headless.demo.qws.xyz"
